[tool.poetry]
name = "facebook-post-downloader"
version = "0.1.0"
description = "Download and archive a single accessible Facebook post"
authors = ["Jag Srawan"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
playwright = "*"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
